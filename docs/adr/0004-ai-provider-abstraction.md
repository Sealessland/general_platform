# ADR 0004：AI Provider 抽象

## 状态

已接受

## 背景

AI Copilot 可能接不同模型厂商，也可能在 CI 中使用 mock，或在本地使用低成本模型。

## 决策

通过统一的 Provider 接口暴露 AI 能力，把厂商细节封装在适配器内部。

## 影响

- 测试可以使用稳定可重复的 Mock Provider
- 生产环境可以在不改业务逻辑的前提下切换模型提供方
- Provider 错误必须先归一化，再传递给上层产品代码
