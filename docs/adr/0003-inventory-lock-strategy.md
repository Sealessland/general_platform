# ADR 0003：库存锁策略

## 状态

已接受

## 背景

内容电商的集中种草会带来短时间的下单流量峰值，系统必须同时处理防超卖和防重复下单。

## 决策

目标方案使用 Redis Lua 进行原子库存预扣，PostgreSQL 事务负责订单与库存锁最终落盘；创建订单时强制要求 `idempotency_key`，并用唯一索引兜底。

## 影响

- Redis 保护热点扣减路径
- PostgreSQL 继续作为最终事实源
- 超时取消、主动取消和退款流程都必须实现库存释放或补偿
